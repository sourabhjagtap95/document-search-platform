# The application, domain and infrastructure packages do not exist yet — they
# arrive on Days 2-5. Layering rules that reference them would otherwise fail with
# "failed to check any classes" instead of passing vacuously.
archRule.failOnEmptyShould=false
